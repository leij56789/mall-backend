<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.PaymentOrderMapper">

    <resultMap id="BaseResultMap" type="com.mall.entity.PaymentOrder">
            <id property="id" column="id" />
            <result property="paymentId" column="payment_id" />
            <result property="orderId" column="order_id" />
            <result property="userId" column="user_id" />
            <result property="amount" column="amount" />
            <result property="paymentMethod" column="payment_method" />
            <result property="status" column="status" />
            <result property="thirdPartyTradeNo" column="third_party_trade_no" />
            <result property="callbackTime" column="callback_time" />
            <result property="expiredAt" column="expired_at" />
            <result property="version" column="version" />
            <result property="createdAt" column="created_at" />
            <result property="updatedAt" column="updated_at" />
            <result property="retryCount" column="retry_count" />
        <result property="prepayId" column="prepay_id" />
        <result property="extInfo" column="ext_info" typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,payment_id,order_id,user_id,amount,payment_method,
        status,third_party_trade_no,callback_time,expired_at,version,
        created_at,updated_at,retry_count,prepay_id,ext_info
    </sql>
    <select id="selectTimeoutNonFinal" resultType="com.mall.entity.PaymentOrder">
        SELECT *
        FROM payment_order
        WHERE status IN
        <foreach collection="statusList" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
        AND expired_at &lt; NOW()
        ORDER BY expired_at ASC
        LIMIT #{limit}
    </select>

</mapper>
