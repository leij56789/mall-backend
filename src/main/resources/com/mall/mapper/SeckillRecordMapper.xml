<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.SeckillRecordMapper">

    <resultMap id="BaseResultMap" type="com.mall.entity.SeckillRecord">
            <id property="id" column="id" />
            <result property="userId" column="user_id" />
            <result property="bookId" column="book_id" />
            <result property="orderId" column="order_id" />
            <result property="status" column="status" />
            <result property="createdAt" column="created_at" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,book_id,order_id,status,created_at
    </sql>
    <insert id="insertByUsername">
        insert into seckill_record (user_id, book_id, status)
        values ((select id from user where username=#{currentUsername}),#{bookId},#{code})
    </insert>
</mapper>
