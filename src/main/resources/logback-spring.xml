<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- ?? 把所有新字段都加上 -->
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS}
                [%X{traceId}] [%X{callSeq}] [%X{spanId}] [%X{parentSpanId}] [%X{userId}] [%X{bookId}]
                %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
<!--<pattern>-->
<!--%d{yyyy-MM-dd HH:mm:ss.SSS}-->
<!--[%X{traceId}] [%X{spanId}] [%X{parentSpanId}]-->
<!--[%X{userId}] [%X{tenantId}] [%X{grayTag}] [%X{bookId}]-->
<!--%-5level %logger{36} - %msg%n-->
<!--</pattern>-->